# ⭕ Крестики-нолики (Tic‑Tac‑Toe) на JavaFX

**Версия**: 1.0-SNAPSHOT  
**Статус**: ✅ игра полностью рабочая  

---

## 📝 О проекте

Это классическая игра **«Крестики-нолики»** с графическим интерфейсом, написанная на **Java** с использованием **JavaFX** и **Maven**.  
Проект создавался в среде **IntelliJ IDEA** — от первой строки кода до последнего теста.

Игра предназначена для двух игроков на одном устройстве. Вы по очереди ставите X и O на поле 3×3.  
Побеждает тот, кто первым выстроит три своих знака в ряд (по горизонтали, вертикали или диагонали).

**Что умеет игра:**
- 🎮 Игра для двух игроков (человек против человека).
- 🖥️ Чистый и понятный интерфейс на JavaFX.
- 🧠 Автоматическая проверка победы и ничьей.
- 💬 Всплывающие уведомления с результатом партии.
- 🚫 Блокировка поля после окончания игры.

---

## 💻 Среда разработки

Проект полностью сделан в **IntelliJ IDEA** (Community Edition) с использованием **JavaFX** и Maven.

**Как всё собиралось:**
1. Создан новый проект через `File → New → Project → JavaFX`.
2. Подключён Maven для удобного управления зависимостями (вместо ручного добавления JAR-файлов).
3. В `pom.xml` прописаны зависимости:
   - `javafx-controls`
   - `javafx-fxml`
4. Интерфейс нарисован с помощью **Scene Builder** (встроенного в IDEA или отдельного приложения).
5. Класс `HelloController` реализует всю игровую логику: ходы, проверку победы, ничью и вывод сообщений.
6. Запуск происходит через кнопку `Run` или командой Maven: `javafx:run`.

**Почему IntelliJ IDEA + JavaFX?**  
Это современный и надёжный стек для десктопных приложений на Java. Он даёт быстрый старт, удобный отладчик и визуальный редактор интерфейса.

---

## 🛠️ Технологии и требования

| Компонент | Версия |
|-----------|--------|
| Java | 9+ |
| JavaFX | 11.0.2 |
| Maven | 3.6+ |
| IntelliJ IDEA | 2020.3+ (любая версия) |

**Основные зависимости (из `pom.xml`):**
- `javafx-controls` – элементы управления (кнопки, панели).
- `javafx-fxml` – загрузка интерфейса из FXML.
- `controls-fx` – дополнительные компоненты (опционально).

---

## 📁 Структура проекта (внутри папки `tic_tac_toe_svoy_cod`)
tic_tac_toe_svoy_cod/
├── .mvn/wrapper/ # Maven Wrapper (не требует установки Maven)
├── src/
│ ├── main/
│ │ ├── java/com/example/tic_tac_toe_svoy_cod/
│ │ │ ├── HelloApplication.java # Точка входа (запускает JavaFX)
│ │ │ └── HelloController.java # Контроллер с игровой логикой
│ │ └── resources/ # FXML-файлы и стили (если есть)
│ └── test/ # (пока пусто, но можно добавить тесты)
├── .gitignore
├── mvnw, mvnw.cmd # Скрипты для Maven Wrapper
└── pom.xml # Конфигурация Maven

text

**Главные классы:**
- `HelloApplication` – загружает FXML и показывает окно.
- `HelloController` – содержит:
  - массив `field[3][3]` для хранения символов
  - переменные `currentPlayer` и `gameActive`
  - методы `buttonClick()`, `checkWin()`, `isDraw()`, `showAlert()`

---

## 🚀 Запуск проекта

### Из командной строки (с Maven Wrapper)

1. Откройте терминал в папке проекта:
   ```bash
   cd fumiklea/tic_tac_toe_java/tic_tac_toe_svoy_cod
Запустите игру:

Windows:

bash
.\mvnw.cmd clean javafx:run
Linux / macOS:

bash
./mvnw clean javafx:run
Из IntelliJ IDEA (проще всего)
Откройте проект в IDEA как Maven-проект.

Найдите класс HelloApplication.

Нажмите зелёную стрелку рядом с main().

Готово – игра запустится.

⚠️ Если JavaFX не стартует, убедитесь, что в pom.xml подключён плагин javafx-maven-plugin. В проекте он есть, поэтому проблем быть не должно.

🎮 Игровой процесс – как играть
Открывается окно с заголовком «Tic Tac Toe!».

На поле 3×3 все клетки пусты.

Первым ходит Игрок X.

Кликайте по любой пустой клетке – туда встанет ваш знак.

Ход переходит к другому игроку.

Игра автоматически определяет:

Победу – если три одинаковых знака в ряд. Появится поздравление.

Ничью – если поле заполнено, но никто не выиграл.

После окончания игры все клетки блокируются.

💡 Совет: Хотите сыграть ещё раз? Пока что нужно перезапустить программу. В будущих версиях обязательно появится кнопка «Новая игра».

📖 Немного об игре «Крестики-нолики» (то, чего нет в других README)
Вы знали, что:

Древние корни – археологи находят похожие игры на досках в Древнем Египте (около 1300 лет до н.э.). Правда, правила до нас дошли не полностью.

Полный перебор – в классических крестиках-ноликах на поле 3×3 всего 255 168 возможных партий, если учитывать порядок ходов. Если не учитывать симметрию – всего 138 уникальных финальных позиций.

Ничья при идеальной игре – если оба игрока не ошибаются, игра всегда заканчивается вничью. Первый игрок может гарантировать себе ничью, а второй – не проиграть.

Стратегия для новичков – самый простой способ не проиграть: занимай центр, а потом углы. Центр даёт больше всего линий (4).

Компьютерный AI – самый простой бот для этой игры пишется на 10-15 строчках кода (рандомные ходы). А идеальный бот (алгоритм минимакс) – уже в 50-100 строк. Некоторые студенты пишут его в качестве учебного задания на втором курсе.

В 2022 году вышел научный труд, где крестики-нолики использовали для обучения нейросетей с подкреплением. Игра настолько проста, что идеально подходит как «полигон» для AI.

Вот почему этот проект – не просто развлечение. Это ваш первый шаг к пониманию:

игрового AI,

событийно-ориентированного программирования (клики мыши),

работы с графическим интерфейсом.

Вы сделали игру, о которой написаны диссертации – и это круто!

🔧 Детали реализации (для любопытных)
Хранение поля:

java
private char[][] field = new char[3][3];
Изначально все ячейки содержат символ  (пробел).

Обработка клика:

По нажатию на кнопку вычисляются её координаты в GridPane (getRowIndex, getColumnIndex).

Если ячейка пуста и игра активна – ставится currentPlayer.

После этого проверяется победа или ничья.

Если никто не выиграл – currentPlayer меняется на другого.

Проверка победы:

Метод checkWin() сканирует 3 строки, 3 столбца и 2 диагонали.

Как только находит три одинаковых непустых символа – возвращает true.

Ничья:

isDraw() возвращает true, если все ячейки не пусты и при этом не было победы.
